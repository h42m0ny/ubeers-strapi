{
  "routes": [
    {
      "method": "GET",
      "path": "/beers",
      "handler": "beers.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/beers/count",
      "handler": "beers.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/beers/:id",
      "handler": "beers.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/beers",
      "handler": "beers.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/beers/:id",
      "handler": "beers.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/beers/:id",
      "handler": "beers.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
